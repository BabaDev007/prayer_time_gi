import 'package:flutter/cupertino.dart';

class PaddingManager{
   var  listViewPadding = EdgeInsets.symmetric(vertical: 25);
   var columnPadding = EdgeInsets.symmetric(horizontal: 30, vertical: 10);
  var titlePadding = EdgeInsets.all(10);
  var prayerTimeWidgetPadding = const EdgeInsets.all(4.0);
}